package dash.component;

import dash.asyntask.ExtractMPD;

import java.util.concurrent.ArrayBlockingQueue;
import java.util.concurrent.BlockingQueue;

/**
 * Created by xuzhiwei on 7/11/15.
 */
public class BlockingQueueWrapper {
    public static BlockingQueue<String> blockingQueue = new ArrayBlockingQueue<String>(10);
    public static BlockingQueue<String> firstQueue = new ArrayBlockingQueue<String>(1);
    public static ExtractMPD extractMPD=null;

}
